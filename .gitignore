node_modules/
logs/
*.log
.env
.env.*

# Local overrides
src/config/local.json

# === STATE FILES: NEVER TRACK ===
# Runtime state is ephemeral. NEVER use `git add -f` on these.
# If state is lost, reconstruct from blockchain — not from git.
state/**
!state/.keep
state-*/
